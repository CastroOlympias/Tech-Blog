<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Aweseome Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header class="header">

      <nav>
        <h1>{{#if loggedIn}}
          <a class="h1" href="/dashboard">Your Blog</a>
          <button id="logout" class="h2 btn-no-style">Logout</button></h2>
          <h1>{{else}}
            <a class="h1" href="/login">Login</a>
            {{/if}}
            </h2>
      </nav>
      <h1>
        <a class="h1" href="/">Awesome Tech Blog</a>
      </h1>
    </header>
    <main class="center">
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>