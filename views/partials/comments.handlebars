<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
    Commented by {{user.username}} on {{format_date created_at}} at {{format_time created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>